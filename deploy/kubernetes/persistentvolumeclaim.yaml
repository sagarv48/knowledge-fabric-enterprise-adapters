apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: enterprise-adapters-approvals
  namespace: enterprise-adapters
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
